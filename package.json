{
	"name": "backendpractice",
	"version": "0.0.2",
	"description": "backend practice with node",
	"license": "MIT",
	"author": "jadera",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js",
		"dev": "node --watch index.js",
		"build:ui": "rm -rf dist && cd ../Notes-front-p3/ && npm run build && cp -r dist ../notes-backend-p3/",
		"deploy": "~/.fly/bin/fly deploy",
		"deploy:full": "npm rum build:ui && npm run deploy",
		"logs:prod": "fly logs",
        "lint": "eslint ."
	},
	"dependencies": {
		"dotenv": "^17.2.2",
		"express": "^5.1.0",
		"mongoose": "^8.18.0",
		"morgan": "^1.10.1"
	},
	"devDependencies": {
		"@eslint/js": "^9.39.1",
		"@flydotio/dockerfile": "^0.7.10",
		"@stylistic/eslint-plugin": "^5.5.0",
		"eslint": "^9.39.1",
		"globals": "^16.5.0"
	}
}
